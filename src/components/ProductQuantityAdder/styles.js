import styled from 'styled-components';

export const ProductQuantityAdderWrapper = styled.div`
  margin-top: 20px;
  > strong {
    display: block;
    margin: 10px 0;
  }

  > form {
    display: flex;
  }
`;
